[package]
name = "workspace-sum"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Dependency-free Rust workflow consumer example"
license = "MIT"
publish = false

[dependencies]
workspace-arithmetic = { path = "../core", version = "0.1.0" }
