//! The gate and the tests as structures: the layers, the absence of import
//! cycles, the step registry, and what holds every step and every refusal.

mod layer_boundaries;
mod step_and_refusal_coverage;
mod step_registry;
